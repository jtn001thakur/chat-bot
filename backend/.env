# Environment
NODE_ENV=development
PORT=3000

# Database (Local for development, replace with real credentials for production)
MONGODB_URI=mongodb://localhost:27017/chat_bot

# JWT Secrets (Dummy values, replace with secure values in production)
JWT_SECRET=DummySecretKey123!
REFRESH_TOKEN_SECRET=DummyRefreshSecret456!

# Socket Server (Local development)
SOCKET_SERVER_URL=http://localhost:9000

# SMS API (Dummy values, replace with real API credentials)
SMS_API_KEY=dummy_sms_api_key
SMS_API_SECRET=dummy_sms_api_secret
